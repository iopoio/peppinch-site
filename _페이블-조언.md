# 페이블 조언 — peppinch-site (2026-07-07)

## 현재 상태

peppinch.com — 후추님 1인 인디해커 스튜디오의 라이브 메인 사이트. 정적 HTML·빌드 없음·Cloudflare Pages 자동배포. 3면 구조: `/`(paper sketch 홈), `/business/`(시네마틱 포트폴리오), `/blog/`(글 9개, 6/13~7/4, 펩랩 브리핑+내 생각 2파일 포맷 정착).
오늘(7/7) 홈·블로그 세이지 그린 리디자인 배포 완료 + QA 6건 중 4건 수정(health 82→96). git 클린, origin/main 동기 상태. 운영 문서(MAP.md·GUIDE.md·DEPLOY.md)가 잘 정비된 편.

## 정리해야 할 것

실제 확인한 것만:

1. **`sitemap.xml` 구식 (SEO 직결)** — 블로그 글 9개 중 4개만 등재. 6/24·6/27·7/1·7/2·7/4 글 5건 누락. 홈 `lastmod`도 2026-05-14로 오늘 리디자인 미반영. 글 발행 절차(GUIDE.md)에 sitemap 갱신이 빠져 있어 계속 벌어질 구조.
2. **`DESIGN.md` 브랜드 SoT가 리디자인 미반영** — 갱신 2026-05-25 상태. 7/7 세이지 그린 리디자인(커밋 f563984·8aef67a) 색·톤이 없음. `README.md` 구조 표("blog = 동일 paper 톤")도 마찬가지. SoT 선언 문서가 실 코드와 어긋난 상태.
3. **`blog/post.html` = 죽은 코드** — `?p=slug`로 `posts/*.md`를 fetch하는 레거시 동적 로더. 사이트 어디서도 참조 없음(전 링크가 `.html` 직행). 짝인 `blog/posts/2026-06-13-all-in-fable5.md`도 `.html` 정본이 따로 있는 잔재. 둘 다 제거 후보(외부에 `post.html?p=` 링크 공유한 적 없는지만 확인).
4. **`blog/posts/assets/character/` PNG 2장** — 영상 생성 테스트용 커밋(커밋 메시지에 "추후 제거" 명시). HTML 어디서도 참조 없는데 배포에 실려 나감. 삭제 또는 아카이브 이동.
5. **`llms.txt` ↔ 홈 카드 불일치** — 홈에는 publace.site·pepstocks.com 카드가 있는데 llms.txt Projects에 Publace·PepStocks 항목이 없음. AI 크롤러용 정보가 반쪽.
6. **`.claude/` 핑퐁 로그 20여 개** — `og_codex_*`, `ogr_build*` 등 `.err`/`.txt`. gitignore돼 있지만 로컬 클러터. 6/15 작업 산출물이라 정리해도 무방.
7. **QA 보류 2건 잔존** — 홈 `#etc` 데드 앵커(대상 id 없음), contact가 mailto 단독. QA 리포트에 Low로 기록만 된 상태.
8. **`inbox/2026-06-20_blog-tag-filter-idea.md`** — "글 6개라 과설계, 쌓이면 재검토" 보류. 지금 9개 — 재검토 시점 도달.

## 더 발전하려면

우선순위 순:

1. **글 발행 스크립트 하나로 sitemap·index 동기화** — 지금 발행 = 파일 2개 생성 + blog/index.html 수동 수정, sitemap은 아예 누락 중. `scripts/`에 이미 파이썬 2개 있으니 `scripts/publish.py` 하나 추가: posts/ 스캔 → blog/index.html 목록·sitemap.xml 자동 재생성. 누락 사고가 구조적으로 사라짐.
2. **RSS/Atom 피드 추가** — 빌드인퍼블릭 블로그인데 구독 경로가 0. 위 스크립트에 feed.xml 생성 몇 줄 얹으면 끝. 재방문 자산의 최소 단위.
3. **블로그 공통 CSS 추출** — 이번 QA에서 터치 타깃 수정 하나에 12개 파일을 고쳐야 했음 = 스타일이 글마다 인라인 복제. `blog/blog.css` 하나로 빼면 다음 스타일 수정부터 1파일. 기존 글은 새 글부터 적용해도 됨.
4. **글별 og:image 자동 생성** — 6/27 글만 전용 이미지, 나머지는 공통 og-image.png. `scripts/render_og.py`가 이미 있으니 제목 넣은 글별 OG 생성으로 확장. X 공유 클릭률에 직접 영향.
5. **태그 필터 1안(클라이언트 JS) 착수** — inbox 메모의 자체 판단 기준(글 쌓이면) 충족. 글마다 `data-tags` + 칩 클릭 필터. 정적 페이지 생성(2안)은 아직 과설계 맞음.

## 앞으로 나아가야 할 방향 (심층)

### 단기 (1~2개월)

- **콘텐츠 리듬이 최대 자산 — 지키기.** 3주에 9글(주 2~3회)은 1인 기준 훌륭한 페이스고, 펩랩→2파일 포맷 파이프라인이 MAP.md에 재발방지 수준으로 문서화돼 있음. 새 기능보다 이 리듬 유지가 우선.
- **SEO 배관 마감.** 글은 쌓이는데 sitemap 누락으로 검색 유입 인프라가 새는 중. 위 1·4번(sitemap 자동화 + 글별 OG) 처리하고 Search Console 색인 상태 확인까지. 콘텐츠 투자 대비 회수 장치가 지금 제일 싸게 고쳐짐.
- **배포 루프 연결.** llms.txt에 X(@peppinch_)가 이미 있고 Think 쪽에 marketing-team v0(블로그 본체 1 → X 파생 1) 루프가 정의돼 있음. 글 9개 중 X 파생이 실제 몇 개 나갔는지 점검하고, 안 돌았다면 이번 주 글부터 복붙 키트 루프 가동.
- **진열대 정직성 유지 + `#etc` 페이지.** EatScan·TickDeck 카드가 /business/로 가는 임시 링크인 건 "작업중" 표기가 있어 괜찮음. 다만 `#etc` 데드 앵커는 작은 etc 진열대 섹션 하나로 해소 — 사이트 컨셉("틈틈이 만든 작은 도구들")과도 맞음.
- **태그 필터 = 제품별 모아보기.** 글에 이미 `article:tag` 메타가 있으니 비용 낮음. 이게 되면 각 제품(퍼블레이스·펩스탁 등)에서 "관련 글" 역링크가 가능해져 제품↔블로그 상호 유입 구조가 생김.

### 중기 (6개월)

- **포트폴리오 → 제품 허브로.** 지금 제품들이 publace.site·pepstocks.com·idaltrip.vercel.app 등 외부에 흩어져 있고 peppinch.com은 진열대 역할만 함. 제품별 서브페이지(예: `/p/publace`)에 스토리 + 태그된 글 목록 + 제품 링크를 모으면, 블로그 글 하나하나가 제품 랜딩의 신뢰 자산으로 축적됨. 도메인 SEO도 peppinch.com에 쌓임.
- **구독자 = 첫 축적 자산.** 현재 방문 성과가 GA 이벤트(like)로만 남고 축적되는 게 0. RSS(단기) 다음은 이메일 캡처 — 정적 사이트지만 Cloudflare Pages Functions로 폼 처리 가능, 아니면 외부 폼 임베드로 시작. "made slow" 톤이면 월 1회 다이제스트면 충분.
- **business 페이지 CTA 강화.** B2B·투자자용 시네마틱 페이지인데 연락 수단이 mailto뿐(QA에서도 지적). 문의 폼 하나 + "어떤 일을 맡길 수 있는지" 구체 항목이 생기면 수익화(외주·협업 문의) 입구가 됨. 1인 lab의 현실적 첫 수익 경로.
- **데이터로 글 방향 조정.** like 이벤트가 GA에 쌓이는 중 — 분기마다 글 유형별(펩랩 브리핑 vs 직접 쓴 글 vs 제품 빌드로그) 반응을 비교해서 리듬 배분 조정. 측정 장치는 이미 있고 회고 루프만 붙이면 됨.
- **빌드 도입은 글 20~30개 시점에 재판단.** 지금 "빌드 없음"은 강점(배포 단순·의존성 0). 다만 인라인 CSS 복제·수동 index 갱신은 글 수에 비례해 아파짐. 스크립트(단기 1번)로 버티다가, 한계 오면 11ty급 최소 SSG로 — 그 전에 미리 도입하는 건 과설계.
